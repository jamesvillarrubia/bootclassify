{
  "name": "bootclassify",
  "version": "0.0.1",
  "description": "Standard bootstrap LESS and SASS overrides adding significantly more class capabilities.  Developed for inclusion where CMS's are heavily template driven (EE or Drupal) or where end users can build HTML but not CSS.  With these additional classes, almost all structure and formatting can be done from within the template and not need specialized css.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jamesvillarrubia/bootclassify.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jamesvillarrubia/bootclassify/issues"
  },
  "homepage": "https://github.com/jamesvillarrubia/bootclassify",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-autoprefixer": "^3.0.0",
    "grunt-contrib-cssmin": "^0.12.3",
    "grunt-contrib-less": "^1.0.1",
    "grunt-contrib-sass": "^0.9.2",
    "grunt-contrib-watch": "^0.6.1"
  }
}
